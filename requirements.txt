# Enterprise Data Platform — Python Dependencies
# Install: pip install -r requirements.txt

# ── HTTP / API ─────────────────────────────────────────────────────────────────
requests==2.32.3

# ── Data processing ────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0          # Parquet engine

# ── Object storage (MinIO / S3) ────────────────────────────────────────────────
boto3==1.34.120

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Kafka (Streaming Layer) ────────────────────────────────────────────────────
kafka-python==2.0.2          # Pure-Python Kafka client (producer + consumer)
confluent-kafka==2.4.0       # High-performance librdkafka-based client

# ── PySpark (Distributed Transform) ───────────────────────────────────────────
pyspark==3.5.1               # Apache Spark Python API

# ── Forecasting ────────────────────────────────────────────────────────────────
statsmodels==0.14.2       # Holt-Winters ExponentialSmoothing
scipy==1.13.1             # statistical utilities used by statsmodels

# ── Orchestration (local dev — use Docker for full Airflow) ───────────────────
# apache-airflow==2.9.1  # uncomment if running Airflow outside Docker

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
